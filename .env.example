# Claude drafting (M3). Get a key at https://console.anthropic.com (needs credit).
ANTHROPIC_API_KEY=sk-ant-...

# Studio password gate. Unset = open (local dev). Set this on Vercel to keep
# strangers from spending your Claude credits via the public URL.
STUDIO_PASSWORD=
